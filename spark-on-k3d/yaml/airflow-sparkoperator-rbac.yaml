apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: airflow-worker-to-spark-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: spark-operator-controller
subjects:
  - kind: ServiceAccount
    name: airflow-worker     # ← 이게 핵심
    namespace: airflow       # ← Airflow의 네임스페이스

