apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-airflow
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-path
  hostPath:
    path: /opt/kafka-on-k3d/airflow/dags # /opt/airflow/dags
  claimRef:
    namespace: airflow
    name: airflow-dags
